# https://adventofcode.com/YYYY/day/DD

def solve(data):
    return data

def parse(content):
    return content

with open('input.txt') as file:
    data = parse(file.read().strip())

    print(solve(data))
